// User roles with resource:action approach
export const roles = {
  // Admin: 'Admin',
};

const rules = {
  // Admin: {
  //   static: ['public-page:visit', 'lead-dashboard-page:visit'],
  // }
};

export default rules;
